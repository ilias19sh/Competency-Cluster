{
	"name": "competency-cluster",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
     "dev": "node scripts/banner.js && concurrently -k -n BACKEND,FRONTEND -c bgBlue.bold,bgMagenta.bold \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm --prefix backend run start:dev",
    "dev:frontend": "wait-on http://localhost:3000 && npm --prefix frontend run dev",
  	"test": "echo \"Error: no test specified\" && exit 1",
		"prepare": "husky",
		"lint": "npx @biomejs/biome check .",
		"lint:fix": "npx @biomejs/biome check --write ."
	},
	"keywords": [],
	"author": " Ilias & Koman",
	"license": "ISC",
	"private": true,
	"workspaces": [
		"*"
	],
	"devDependencies": {
		"@biomejs/biome": "2.3.12",
		"chalk": "^5.6.2",
		"concurrently": "^9.2.1",
		"husky": "^9.1.7",
		"wait-on": "^9.0.3"
	}
}
